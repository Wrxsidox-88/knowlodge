# knowlodge 更新日志

## 1.2.1
- 更新日志与系统介绍（项目 README）改为 **Markdown 自动渲染**（支持标题、列表、代码块、行内强调、数学公式等），不再显示未渲染的原始文本。

## 1.2.0
- **版本与更新面板增强**：新增按钮可查看「该版本更新日志」与「系统介绍（项目 README）」；新增**强制更新**（即使未检测到新版本也可从仓库强制同步，需密码验证）。
- **安全加强**：除「检查更新」外，所有更新相关操作（保存更新设置、执行更新、强制更新）均需**密码验证**。
- **更新进度实时显示**：更新过程中（直至重启前）以 WinUI 进度条实时显示进度百分比与步骤提示，可轮询查看进度。
- **版本号动态获取**：系统「当前版本」「服务器版本」均直接读取 `.release/.version`，不再硬编码。

## 1.1.0
- **新增「版本与更新」**：内置 GitHub 仓库地址（可在设置修改），定时访问仓库对比 `.release/.version` 版本文件，检测到更高版本时提供更新。
  - 更新方式支持**增量对比更新**（git-tree 逐文件对比 SHA、仅下载差异文件，应用前需**密码验证**）与**完整包替换**。
  - 更新由**独立进程**执行：备份 → 替换 → 前端构建 → PM2 重启 → 健康检测 → **异常自动回滚**，防止数据损坏；期间若存在运行中的任务会等待其结束/失败。
  - 自动更新策略：仅检测提醒 / 检测并下载 / 直接完成更新（可设置检查间隔、代理）。
- **设置页新增「版本与更新」子栏**（Windows 更新风格）：查看当前版本、检查更新、更新日志、差异文件预览、应用更新、更新历史与结果。
- **账户系统**：登录、当前账户信息展示、密码修改（显示密码二次确认）、退出。
- **数据管理二次确认**：删除等危险操作需二次确认（密码验证弹窗）。
- **8 种页面切换动画**：在「设置 → 外观与动画」可配置（幻灯片/淡入/上浮/钻取/连续缩放等）。
- **图标字体本地化**：界面由系统图标字体替代 Emoji，随主题切换。
- **深色/浅色主题切换**；弹窗层置于最上层避免遮挡标题栏与误触。
- AI 智能问答还原为**一次性非流式**渲染，规避流式性能问题。
- Token 计量：可查看 token 消耗趋势/总量/性能，设置额度与临时增加（**需密码认证**）。

## 1.0.0
- 初始版本：核心学习中心 Windows 风格界面、智能问答、知识库、AI 分析、诉求与错题本、设置中心。
