# knowlodge 更新日志

## 1.6.0
- **考试管理支持排名录入与排名分析**：登记考试/练习时可填写**年级排名**与**班级排名**（选填），历史记录表格新增两列展示；大型考试总览（多科目联合分析）每科右侧同步显示年级/班级排名。
- **成绩趋势图支持排名指标**：考试页与学情分析页的趋势图新增指标切换（得分率 % / 年级排名 / 班级排名），排名视图自动翻转 Y 轴（数值越小越靠上），缺失排名的点自动断开连线。
- **设置页新增「目标成绩」**：可分别设定**总分目标**与**每科目标**（目标分数、满分、目标年级排名、目标班级排名），支持一键「AI 根据最近情况给建议」——AI 根据最近 500 条考试记录、最近一次大考总分与目标现状，给出分数与排名建议并自动填入（可修改后保存）；未配置 AI 或暂无考试数据时给出离线提示。
- **学情分析绘制目标线与近期平均线**：成绩波动趋势图新增时间窗口筛选（全部/近 30/90/180 天/近一年），目标线（黄色虚线，来自设置页目标成绩，得分率按目标分数/满分换算、排名视图取目标排名）与**近期平均线**（点线，取筛选窗口内各科平均值）叠加绘制；时间筛选窗口内的数据重新计算。

## 1.5.0
- **重写更新功能**：恢复并完善两种可选更新模式，执行更新时可在弹窗中自由选择（默认模式可在设置保存）：
  - **增量更新**：对比仓库差异，仅替换发生变更/新增的文件，并删除仓库中已移除的文件；应用前可在弹窗内预览完整差异清单（新增/修改/删除分组、彩色标注）。
  - **全量更新**：下载仓库完整包，直接覆盖全部本地代码，更新后与仓库完全一致。
  - 无论哪种模式，`server/data` 数据、`.env` 配置、依赖目录与密钥文件一律不受影响；执行前均需密码验证。
- **专用更新模块接管**：确认更新后由独立进程完全接管，主进程不参与执行，流程为 下载 → 备份 → 替换/删除 → 版本文件 → 依赖安装（package.json 变化时按需）→ 前端构建 → PM2 重启 → **自检**（健康检测）。
- **紧急回滚**：构建失败、重启失败或自检超时，自动恢复备份、重新构建并重启，服务回到更新前状态；备份仅保留最近 5 份，自动清理旧备份。
- **前端实时反馈**：更新全程进度条显示阶段与百分比（下载速度/替换/构建/重启/自检/回滚）；检测到更新模块返回**更新成功**后页面自动刷新，失败则展示原因且不刷新。

## 1.4.9
- **修复「Token 计量」弹窗一闪而过并跳转空白页**：此前点击「Token 计量…」后弹窗刚显示就消失、页面变空白，根因是弹窗内趋势图的面积路径计算把 `chartPolyline()` 返回的**字符串**当作数组调用了 `.join(' ')`，一旦累计趋势数据达到 2 天以上（折线图开始渲染）即抛 `TypeError: pts.join is not a function`，导致 Vue 渲染崩溃空白页；现已改为按字符串拼接闭合面积路径，趋势图正常显示。
- **修复开发者模式切页后浮动面板消失**：此前「开发者选项」面板只存在于设置页组件内，切换到其他页面后随组件卸载而消失，需再次连点 5 次才重新出现；现改为**全局挂载**（App 壳层 + 全局状态 `devState`），开发者模式开启后面板在所有页面持续显示，切页不再丢失，刷新后仍自动恢复。
- **优化开发者模式开启判定**：连点 5 次「系统版本」时，未开启状态不再误弹「已开启」提示，统一按实际状态弹「开启确认」或「已开启」提示。

## 1.4.8
- **修复「开发者选项」把导航/页眉挤上移**：开发者选项改为**固定于页面右下角的浮动面板**，不再占据整块页面布局，也不会因出现在底部而把页面内容推向页眉、遮挡展开图标。该面板悬浮于页面右下，可正常展开/收起。

## 1.4.7
- **修复「开发者选项」栏无法自由伸缩**：此前用单向 `:IsExpanded` 绑定导致展开收缩状态被锁死（怎么点都是缩回）；改为 `v-model:IsExpanded` 双向绑定，点击可正常展开/收起，默认展开。

## 1.4.6
- **修复开发者模式弹窗一闪而过**：此前已开启状态下再次连点触发的「开发者模式已开启」弹窗会在点击蒙层空白时关闭，快速连点常令弹窗一闪而过；现移除蒙层点击关闭（只能点「知道了」关闭）。
- **确保开发者选项栏显示**：开发者模式已开启时「开发者选项」区域默认展开并自动滚动定位，即便状态加载略有延迟也会强制显示该面板。

## 1.4.5
- **开发者模式体验优化**：
  - 开启成功后开发者选项区域自动滚动定位显示，不再"看不到选项"。
  - 已开启状态下再次连点触发时，弹出「开发者模式已开启」提示，不再重复进入开启流程。
  - 修复后端密码校验缺陷：开启开发者模式/清空数据时，密码错误现在会正确拒绝（此前因逻辑缺陷可能放行）。

## 1.4.4
- **修复保存更新设置密码验证失败**：此前前端把密码包进 `values` 内、后端却从请求顶层取密码，导致始终读不到密码而提示"密码验证失败"；现改为前端顶层传参、后端兼容兼容取值，正确密码可正常保存设置（已实测正确密码 → `ok:true`，错误密码 → 403）。

## 1.4.3
- **彻底解决"卡在重启页面"**：前端一旦检测到更新进入「重启」/「健康检测」阶段，就立即开始等待服务恢复（轮询 `/health`），一旦恢复正常**自动刷新页面**，不再等待更新进程跑完；即使 PM2 命令挂起，只要系统已恢复到正常状态即会刷新退出。
- **统一使用完整更新**：移除「增量对比」更新方式，手动更新与自动更新一律以**完整包替换**执行；相关下载命令均带超时兜底，不再无限挂起。
- **界面简化**：移除「查看将更新的文件」与「更新方式」选择项。

## 1.4.2
- **彻底修复强制更新进度条消失/卡住**：此前更新刚发起那一瞬前端读到空闲进度就停掉了轮询，导致下载阶段进度条不出现、只显示"正在拉取代码"并干等；现改为空闲/初始状态绝不停止轮询，下载阶段起即可看到进度条与实时速度（MB/s）。
- 后端下载相关命令增加超时兜底（获取包大小、实际下载）。
- 新增自动化端到端测试（`server/data/test_update_run.mjs`、`test_download.mjs`）：覆盖"备份→替换→版本→构建→重启→健康→完成"全流程与下载实时进度，均已通过。

## 1.4.1
- **修复更新重启阶段卡死**：PM2 重启命令增加超时（不再无限挂起）；命令未返回时不再阻塞也不误回滚，改由健康检测判定，服务恢复后更新顺利完成并正确结束进度。
- **自动刷新防抖**：仅在更新确实进入运行态后结束才触发页面自动刷新，避免启动瞬间误刷新。

## 1.4.0
- **修复"一直在重启"**：更新进程的健康检测改用系统真实监听端口（此前可能用默认端口而探测失败，导致长时间卡在重启步骤）。
- **更新日志弹窗**：更新期间可点击「更新日志」查看后台详细输出（构建 / 重启等命令输出，仅与更新相关），自动滚动最新内容。
- **更新完成自动刷新**：服务重启并恢复健康后自动刷新页面，无需手动刷新。

## 1.3.0
- **更新过程全程实时进度**：下载阶段实时显示**下载进度与速度**（已下载 MB / 总量 MB + MB/s），解压、替换、构建、重启、健康检测各阶段均有百分比与步骤提示；总量未知时进度条显示为不确定状态。
- **修复"正在重启"停滞**：健康检测期间实时显示剩余等待时间（第 N 次 / 剩余 s），成功 / 失败 / 回滚后状态明确结束，不再停留在"正在重启"。
- **强制更新**：不做任何一致性检查，直接从仓库拉取完整包覆盖本地（无论仓库是否一致），需密码验证。

## 1.2.1
- 更新日志与系统介绍（项目 README）改为 **Markdown 自动渲染**（支持标题、列表、代码块、行内强调、数学公式等），不再显示未渲染的原始文本。

## 1.2.0
- **版本与更新面板增强**：新增按钮可查看「该版本更新日志」与「系统介绍（项目 README）」；新增**强制更新**（即使未检测到新版本也可从仓库强制同步，需密码验证）。
- **安全加强**：除「检查更新」外，所有更新相关操作（保存更新设置、执行更新、强制更新）均需**密码验证**。
- **更新进度实时显示**：更新过程中（直至重启前）以 WinUI 进度条实时显示进度百分比与步骤提示，可轮询查看进度。
- **版本号动态获取**：系统「当前版本」「服务器版本」均直接读取 `.release/.version`，不再硬编码。

## 1.1.0
- **新增「版本与更新」**：内置 GitHub 仓库地址（可在设置修改），定时访问仓库对比 `.release/.version` 版本文件，检测到更高版本时提供更新。
  - 更新方式支持**增量对比更新**（git-tree 逐文件对比 SHA、仅下载差异文件，应用前需**密码验证**）与**完整包替换**。
  - 更新由**独立进程**执行：备份 → 替换 → 前端构建 → PM2 重启 → 健康检测 → **异常自动回滚**，防止数据损坏；期间若存在运行中的任务会等待其结束/失败。
  - 自动更新策略：仅检测提醒 / 检测并下载 / 直接完成更新（可设置检查间隔、代理）。
- **设置页新增「版本与更新」子栏**（Windows 更新风格）：查看当前版本、检查更新、更新日志、差异文件预览、应用更新、更新历史与结果。
- **账户系统**：登录、当前账户信息展示、密码修改（显示密码二次确认）、退出。
- **数据管理二次确认**：删除等危险操作需二次确认（密码验证弹窗）。
- **8 种页面切换动画**：在「设置 → 外观与动画」可配置（幻灯片/淡入/上浮/钻取/连续缩放等）。
- **图标字体本地化**：界面由系统图标字体替代 Emoji，随主题切换。
- **深色/浅色主题切换**；弹窗层置于最上层避免遮挡标题栏与误触。
- AI 智能问答还原为**一次性非流式**渲染，规避流式性能问题。
- Token 计量：可查看 token 消耗趋势/总量/性能，设置额度与临时增加（**需密码认证**）。

## 1.0.0
- 初始版本：核心学习中心 Windows 风格界面、智能问答、知识库、AI 分析、诉求与错题本、设置中心。
